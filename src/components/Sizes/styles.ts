import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {

    },
    
    btnSize: {
        width: 45,
        height: 45,
        borderWidth: 2,
        borderRadius: 45,
        justifyContent: "center",
        alignItems: "center",
        borderColor: "rgba(126, 144, 166, 0.75)"
    },
    btnText: {
        color: "rgba(126, 144, 166, 0.75)",
        fontSize: 16
    }
})

export default styles